---
description: Slökkvitæki safety net and deploy guards
alwaysApply: true
---

Read `docs/ORYGGISNET.md` before changing any guarded path.

Run `node tools/audit-all.cjs` before every push.

Never run `node deploy.js`. Deploy is git push only. Pull the default branch first.

Route invoice OUT (`10`/`233`/`254`), kennitala (`121`/`pos.js`), `payday-push`, and readiness (`153`/`187`) through `netvordur`.
