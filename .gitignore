# Node
node_modules/
npm-debug.log*

# CI build output — assembled by build-dist.js, deployed via the Netlify CLI
dist/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*~

# Claude Code local settings (but keep shared skills + shared settings version-controlled)
.claude/*
!.claude/skills/
!.claude/skills/**
!.claude/settings.json

# Local outputs / scratch
/tmp/
/scratch/
*.bak

# Local-only PII / source assets — never commit, never deploy
_customer-import-data.json

# Supabase data backups — local restore points; never push (contain
# customer PII and would bloat the repo). Run `node backup-supabase.mjs`
# to refresh.
backups/

# One-shot scratch scripts and data dumps used during data migrations
tmp_*.mjs
tmp_*.json
tmp_*.txt

# 2026-05-18/19 data sync working files — PII (kennitalas, áminning notes)
# and ephemeral migration scripts. Keep local only.
_pdf_master_customers.json
_sheet_master_data.json
_sheet_equipment.json
_dk_useful.json
_unmatched_*.json
_stage*.json
_*_import.py
_create_unmatched.py
_apply_chains.py
_*_import_log.json
STAGE3-REMAINING.json
_user-screenshot.png
_pdf/
_barcode-photo.jpg
_barcode2.jpg
_logo2.jpg
_pricelist1.jpg
_pricelist2.jpg
_reikningar.png
_extract_logo.cjs

# Netlify CLI cache (if you ever install it)
.netlify/
# Netlify dev / edge-bundler (Deno) lockfile — local dev artifact
deno.lock

# Stadbundid session-minni (memsearch) - inniheldur vinnugogn, ALDREI i git
.memsearch/

# graphify skyndiminni + útgefinn grafur — endurbyggt með `graphify update .`
# á hverri vél. Slæddist inn í eitt commit af skrifborðsvélinni (176 þúsund
# línur af AST-skyndiminni) — á aldrei heima í git.
graphify-out/

# Keep shared subagents version-controlled
!.claude/agents/
!.claude/agents/**

# responsive-craft live preview artifact (auto-copied into serve dir)
_responsive-preview.html

# 2026-08-28 — staðbundnar yfirferðarskrár með kennitölum viðskiptavina.
# Mikilvægt: auto-sync á öðrum vélum má ALDREI taka þessar með.
_luna_*
_open_items.md
# Einnota python-skriftur úr gagnaflutningi (sama regla og tmp_*.mjs/json/txt)
tmp_*.py

# playwright-cli vinnuskrár (uppsett 2026-08-28)
.playwright-cli/
