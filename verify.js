// verify.js — referenced by `npm run verify`. Stub for now.
// TODO: implement deploy verification (e.g. fetch live site, sanity-check
// expected scripts load and key globals exist). For the moment this just
// reports OK so the npm script doesn't error.
console.log('verify.js: stub — no checks implemented yet');
